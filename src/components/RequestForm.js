const RequestForm = ({ product, onClose, onSubmit }) => {
  const [formData, setFormData] = useState({
    pitchMessage: '',
    proposedOutlet: '',
    estimatedReach: '',
    plannedAngle: '',
    estimatedPublishDate: ''
  });

  // Form implementation
  return (
    <div className="fixed inset-0 bg-black bg-opacity-50 flex items-center justify-center z-50">
      {/* Request form modal */}
    </div>
  );
};
